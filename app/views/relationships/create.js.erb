$("#user_<%= user.id %> .follow-btn").html("<%= j(render "relationships/followbtn", user: user) %>");
$(".followings-count").html("<%= user.followings.count %>");
$(".followers-count").html("<%= user.followers.count %>");